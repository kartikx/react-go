apiVersion: apps/v1
kind: Deployment
metadata:
  name: doc-agent
  labels:
    app: doc-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doc-agent
  template:
    metadata:
      labels:
        app: doc-agent
    spec:
      containers:
      - name: doc-agent
        image: doc-agent:latest
        imagePullPolicy: Never  # For local testing
        ports:
        - containerPort: 8080
        env:
        - name: AGENT_TYPE
          value: "doc"
        - name: PORT
          value: "8080"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: doc-agent-service
spec:
  selector:
    app: doc-agent
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
